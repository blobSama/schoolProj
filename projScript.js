// JavaScript source file
function checkForm() {
    if (!checkPic) {
        document.write(whoops, sorry);
    }
}

function checkPic(getElementById(myfile)) {
    if (typeof.getElementByType(myfile)==".jpg" {
        return true;
    }

    else {
        return false;
    }
}